@using demo.Service
@model ArticleViewModel

<div id="article">
    @Html.ValidationSummary(true, string.Empty, new { @class = "text-danger" })
    <input type="hidden" id="Id" name="Id" value=@Model.Id />
    <div class="glyphicon-info-sign basic-infos">
        <div>
            @Html.EditorFor(model => model.Title, new { htmlAttributes = new { @class = "form-control", placeholder = "Title" } })
            @Html.ValidationMessageFor(model => model.Title, string.Empty, new { @class = "text-danger" })
        </div>
        <br />
        <div>
            @Html.TextAreaFor(model => model.Description, new { htmlAttributes = new { placeholder = "Description" } })
        </div>
    </div>
    @if (Model.PagesGlobe != null)
    {
        { Html.RenderPartial("~/Views/Page/EditPages.cshtml", Model.PagesGlobe); }
    }
</div>