@using demo.Service;

@model PageViewModel


<div class="article-page">
    <div class="page-edit">
        <div class="save-page">
            <a class="save-page" href="@Url.Action("Save", "Page")">
                <span class="icon-cloud-upload"></span>
            </a>
            <a class="delete-page">
                <span class="icon-bin delete-page"></span>
            </a>
        </div>
        <div id="pageinfos">
            @{ Html.RenderPartial("~/Views/Page/EditInfos.cshtml", Model); }
        </div>
    </div>
    @{ Html.RenderPartial("~/Views/Page/Delete.cshtml", Model); }
    <div class="loading hide-loading" id="page-loading"><div><span>Loading page content...</span></div></div>
</div> 